# Favicons

Place your favicon files in this directory. Typical files include:

- `favicon.ico` – legacy fallback (16×16 or 32×32)
- `favicon.svg` – modern scalable favicon
- `apple-touch-icon.png` – 180×180 for iOS home screen

These assets are included when the package is installed and can be referenced from consuming projects.
